<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SofitTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SofitTask" parent="Base.Theme.SofitTask" />
    <style name="Base.Widget.Design.TabLayout" parent="android:Widget">
        <item name="tabBackground">@drawable/tab_color_selector</item>
        <item name="tabSelectedTextColor">@drawable/tab_selector</item>
        <item name="tabIconTint">@drawable/tab_selector</item>
        <item name="tabIndicatorHeight">3dp</item>
    </style>
</resources>